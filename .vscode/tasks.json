{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "darklua process src/init.lua build/build.lua --config darklua.json"
        },
        {
            "label": "devbuild",
            "type": "shell",
            "command": "darklua process src/init.lua build/build.lua --config debug.json"
        }
    ]
}