Run this to build:

darklua process src/init.lua build/export.lua --config darklua.json

or

darklua process src/init.lua build/export.lua --config debug.json

Shortcuts:

Use CTRL + P

You can search for functions and stuff by typing in @ for the popup
You can access commands by typing in > in the popup
https://www.youtube.com/watch?v=ifTF3ags0XI

RUN >Run Task
With devbuild
or
build

Look at the snippet "RCL" for checkbox loops, helpful.
